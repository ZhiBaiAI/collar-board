# 2026-09 变更时间线

> 真实记录由 `collar-changelog` 在 commit 时追加。

### 2026-09-17 · feature · 接入 collar-sdd v1.0.0 骨架 + 解析层跟进 Delta 四段/提案/任务层

- **Spec**：— （仓库建制 + 解析能力升级）
- **影响面**：新增 `AGENTS.md`/`collar.yaml`/`VERSION`/`docs/` 六模块/`skills/`/`scripts/collar-*.sh`+`hooks/`；`src/parse/`（ac、specs、model、findings）、`src/fs/collect.js`、`src/ui/`（format、map、overview）、`test/`
- **变更**：① 仓库建制：按下游身份接入 collar-sdd v1.0.0——AGENTS.md/collar.yaml 已填本项目内容，docs 六模块骨架就位，scripts/skills 属上游资产（后续升级走 `collar-sync.sh`），collar-sdd 登记进 vendor SOURCES。② 解析升级：`parseDelta` 支持 patch §⑥ 四段（含 RENAMED FROM/TO 配对）并兼容旧平铺格式；新增 PROPOSAL-NNN 提案解析；新增 §7/§8 实施任务清单解析；收集根目录 VERSION 显示模板版本。③ 结构事实新增两条机械核对：delta 引用主文档不存在的 AC（S5 同口径）、「已验证」但任务未全勾；已收敛/已废弃 patch 不再计收敛阈值与反向指针检查（dogfooding：该检查在 collar-sdd 示范数据上真实抓到 AC-P001-4 未覆盖，上游已修）。
- **破坏性**：无——解析层向后兼容旧格式 patch
- **关联**：—

### 2026-09-17 · feature · 结构事实口径对齐 S5/S7 + 新增「在途变更」视图 + 装配检查

- **Spec**：— （看板能力升级）
- **影响面**：`src/parse/`（ac、specs、findings、model）、`src/fs/collect.js`、`src/ui/`（app、views/inflight、views/findings）、`test/`、`README.md`、`AGENTS.md`
- **变更**：① S5 七类 delta 一致性核对全接入看板（块内重复/跨块冲突/FROM-TO 不配对/TO 非 AC-PNNN-N/段外孤儿行/标题拼错/TO 撞主文档编号），产出 delta-issue 事实。② 收集层扩容到 skills/、scripts/collar-*、scripts/hooks/、.github/workflows/、README*；达上限截断时产出 collection-truncated 显式声明。③ 新核对项：S7 会话指代词、spec 侧「已验证但任务未勾」、提案目标模块悬空、负责人未认领、patch 缺覆盖范围、工具链缺失、CI 未接 collar-check、VERSION 缺失、质量门禁占位符。④ 每条事实附「修法：」一行。⑤ 新视图「在途变更」：未收敛 patch（≥14 天标超观察期）/待审提案/在途 sunset/结构缺口 + Next 建议，口径同 collar-status.sh。⑥ 工程：按目录分桶 O(N)、反向指针改用 mask 后正文、hasScopeSection 接入检查。
- **破坏性**：无——只增加视图与核对项
- **关联**：—

### 2026-09-17 · feature · 「架构与排障」视图 + 各视图筛选

- **Spec**：— （功能完整度补齐：架构基线与排障知识之前没有被解析利用）
- **影响面**：新增 `src/parse/runbook.js`、`src/ui/views/runbook.js`；`src/ui/`（app、map、timeline、inflight）、`src/parse/model.js`、`README.md`、`AGENTS.md`、`test/`
- **变更**：① 解析 docs/architecture/README.md「结构视图」——模块依赖/接口契约/数据模型各小节结构化呈现（表格渲成表格、依赖图等宽）。② 解析 docs/runbook/troubleshooting.md——剧本索引（症状关键词→剧本）与五段式剧本条目（现象/根因/解法/预防/证据），关键词关联到条目可点选过滤。③ 各视图补筛选：决策按状态（已有）、排障按症状、时间线按类型/破坏性、在途按变更类型、业务地图按域、结构事实按严重度（已有）。
- **破坏性**：无
- **关联**：—

