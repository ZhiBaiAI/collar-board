# 2026-09 变更时间线

> 真实记录由 `collar-changelog` 在 commit 时追加。

### 2026-09-17 · feature · 接入 collar-sdd v1.0.0 骨架 + 解析层跟进 Delta 四段/提案/任务层

- **Spec**：— （仓库建制 + 解析能力升级）
- **影响面**：新增 `AGENTS.md`/`collar.yaml`/`VERSION`/`docs/` 六模块/`skills/`/`scripts/collar-*.sh`+`hooks/`；`src/parse/`（ac、specs、model、findings）、`src/fs/collect.js`、`src/ui/`（format、map、overview）、`test/`
- **变更**：① 仓库建制：按下游身份接入 collar-sdd v1.0.0——AGENTS.md/collar.yaml 已填本项目内容，docs 六模块骨架就位，scripts/skills 属上游资产（后续升级走 `collar-sync.sh`），collar-sdd 登记进 vendor SOURCES。② 解析升级：`parseDelta` 支持 patch §⑥ 四段（含 RENAMED FROM/TO 配对）并兼容旧平铺格式；新增 PROPOSAL-NNN 提案解析；新增 §7/§8 实施任务清单解析；收集根目录 VERSION 显示模板版本。③ 结构事实新增两条机械核对：delta 引用主文档不存在的 AC（S5 同口径）、「已验证」但任务未全勾；已收敛/已废弃 patch 不再计收敛阈值与反向指针检查（dogfooding：该检查在 collar-sdd 示范数据上真实抓到 AC-P001-4 未覆盖，上游已修）。
- **破坏性**：无——解析层向后兼容旧格式 patch
- **关联**：—

